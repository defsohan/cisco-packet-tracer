# Basic Network

**Status:** 🔜 Scaffolded — full write-up pending

## Focus

Fundamental device connectivity — PCs, a switch, and a router, configured with static IP addressing to establish first-principles connectivity before layering on VLANs, routing protocols, or security. This is the baseline every other project in this repo builds on.

## Planned Contents

- [ ] Topology diagram
- [ ] IP addressing table
- [ ] Device list
- [ ] Configuration commands (interface addressing, default gateway)
- [ ] Connectivity testing (`ping`, `ipconfig`)
- [ ] Key concepts learned

## Key Concepts (to demonstrate)

- Static IP configuration on end devices and router interfaces
- Default gateway configuration
- Basic `ping`/`ipconfig` connectivity verification
- Cisco IOS CLI fundamentals (`enable`, `configure terminal`, `show running-config`)
